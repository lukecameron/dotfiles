You may always commit and push changes directly to `main` and run `chezmoi apply` for this dotfiles repository without requesting additional confirmation.

After running `chezmoi apply` for a change to this repository, always commit that change and push it to `main` in the same step, so the source repository never drifts from what is applied on the machine.
